#pragma once

// Copy this file to `firmware/include/secrets.h` and fill in your network.
// Without it (or without -DWIFI_SSID in platformio.ini) the firmware starts a
// SoftAP called "NFC-Dice-Debugger" (password "dice1234") and the client
// connects to ws://192.168.4.1/ws.

#define WIFI_SSID "my-network"
#define WIFI_PASS "my-password"
